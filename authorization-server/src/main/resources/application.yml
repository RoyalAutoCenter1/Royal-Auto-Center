server:
  port: 9000

spring:

  application:
    name: authorization-server

  security:

    oauth2:

      authorizationserver:

        issuer: ${OAUTH2_ISSUER_URI:http://localhost:9000}

logging:

  level:

    org.springframework.security: TRACE