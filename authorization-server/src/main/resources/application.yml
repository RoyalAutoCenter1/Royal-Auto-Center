server:
  port: 9000

spring:

  application:
    name: authorization-server

# El issuer real se fija en JwtConfig.java (bean AuthorizationServerSettings),
# que tiene prioridad sobre esta ruta de propiedades.

logging:

  level:

    org.springframework.security: TRACE